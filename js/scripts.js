function beepboop(number){
  let returnedOutput= [];
  if (i.toString().includes("1")) {
    returnedOutput.push("Beep!")
  } else {
    returnedOutput.push(i)
  for (let i = 0; i <= number; i++) {
  if (number === "0") return returnedArray
  }}
  return returnedOutput;
}